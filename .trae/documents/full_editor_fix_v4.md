# 完整编辑器修复 v4 - 简化代码结构

## 问题诊断

经过深入分析，发现以下问题：

1. **JavaScript 语法错误**：虽然第 843 行的代码看起来正确，但可能存在隐藏的语法错误
2. **injectEditorScript 中的模板字符串**：使用了大量内联模板字符串，可能导致解析问题
3. **代码结构复杂**：多层嵌套的模板字符串和事件处理使代码难以维护

## 修复方案

将重写 `main.js`，采用以下策略：

1. **移除 injectEditorScript 中的复杂内联脚本**
2. **简化 iframe 交互逻辑**
3. **使用外部脚本文件注入编辑器功能**
4. **简化模板字符串的使用**

## 实施步骤

1. 重写 main.js，简化代码结构
2. 创建外部编辑器脚本文件
3. 测试验证
